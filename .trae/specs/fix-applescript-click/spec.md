# 修复 AppleScript 点击功能

## 问题分析
- AppleScript 命令在终端能正常工作（用户确认"鼠标消失"）
- Electron 应用调用时可能坐标系统不匹配
- 需要确保坐标在屏幕范围内

## 修复方案
1. 简化 AppleScript 语法，直接使用 `click at` 
2. 添加坐标验证，确保在屏幕范围内
3. 添加调试日志，查看实际执行的坐标
4. 使用更可靠的点击方式

## 技术细节
- macOS 坐标系统：左上角为 (0, 0)
- 需要获取屏幕尺寸来验证坐标
- AppleScript 的 `click at` 是最简单可靠的方式
